declare module 'react-native-url-polyfill/auto' {
  // Essa é uma importação com efeitos colaterais, então a interface do módulo é vazia.
  // O objetivo é apenas dizer ao TypeScript que ele existe.
}